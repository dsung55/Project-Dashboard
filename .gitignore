# Dependencies — regenerated by "npm install"
node_modules/

# Build output — produced by electron-builder
dist/

# Personal data — each user keeps their own locally
data/projects/
data/projects.json
data/tasks.json
data/config.json
data/backgrounds/

# Claude Code project settings (local paths, user-specific permissions)
.claude/
