{
  "name": "project-dashboard",
  "version": "1.6.0",
  "description": "Local project management dashboard",
  "main": "electron.js",
  "scripts": {
    "start": "node server.js",
    "electron": "node -e \"const {spawn}=require('child_process');spawn(require('electron'),['.'],{stdio:'inherit'})\"",
    "build-icons": "node scripts/generate-icons.js",
    "dist": "npm run build-icons && electron-builder --win --publish never",
    "dist:mac": "npm run build-icons && electron-builder --mac --publish never",
    "release": "npm run build-icons && electron-builder --publish always"
  },
  "dependencies": {
    "electron-log": "^5.4.3",
    "electron-updater": "^6.8.3",
    "express": "^4.18.2",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "electron": "^41.3.0",
    "electron-builder": "^26.8.1",
    "png-to-ico": "^2.1.8",
    "sharp": "^0.33.5"
  },
  "build": {
    "appId": "com.projectdashboard.app",
    "productName": "Project Dashboard",
    "asar": false,
    "icon": "build/icon.png",
    "win": {
      "target": "nsis",
      "icon": "build/icon.ico",
      "requestedExecutionLevel": "asInvoker"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Project Dashboard",
      "artifactName": "${name}-Setup-${version}.${ext}"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "universal"
          ]
        }
      ],
      "icon": "build/icon.icns",
      "category": "public.app-category.productivity",
      "artifactName": "Project Dashboard (Mac).${ext}"
    },
    "dmg": {
      "title": "Project Dashboard",
      "window": {
        "width": 540,
        "height": 380
      }
    },
    "files": [
      "**/*",
      "!data/**",
      "!.git/**",
      "!.claude/**",
      "!*.md",
      "!CHANGELOG.md"
    ],
    "publish": {
      "provider": "github",
      "owner": "dsung55",
      "repo": "Project-Dashboard"
    }
  }
}
